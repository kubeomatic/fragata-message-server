apiVersion: v1
kind: Secret
metadata:
  namespace: {{ .Release.Name }}
  name: {{ .Values.common.name }}
type: Opaque
data:
  ks.p12: {{ .Values.secret.keyStore }}
  ks.secret: {{ .Values.secret.keyStorePAss | b64enc }}
